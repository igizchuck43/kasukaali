<x-layouts.admin :title="$title"><h1 class="text-3xl font-extrabold">{{ $title }}</h1><div class="mt-6 space-y-3">@foreach($items as $item)<div class="rounded-lg border border-borderSoft bg-white p-5 shadow-soft"><div class="font-bold">{{ $item->title ?? $item->question ?? $item->name }}</div><div class="text-sm text-muted">{{ $item->status }}</div></div>@endforeach</div><div class="mt-6">{{ $items->links() }}</div></x-layouts.admin>
