<x-layouts.admin title="Pending Approvals"><h1 class="text-3xl font-extrabold">Pending Approvals</h1><div class="mt-6 space-y-4">@foreach($users as $user)<div class="rounded-lg border border-borderSoft bg-white p-5 shadow-soft"><div class="flex flex-col justify-between gap-4 sm:flex-row"><div><div class="font-bold">{{ $user->name }}</div><div class="text-sm text-muted">{{ $user->email }} · {{ $user->city }}</div></div><div class="flex gap-2"><form method="POST" action="{{ route('admin.users.approve', $user) }}">@csrf<x-admin.action-button>Approve</x-admin.action-button></form><form method="POST" action="{{ route('admin.users.reject', $user) }}">@csrf<input type="hidden" name="reason" value="Registration details did not meet approval requirements."><x-admin.action-button>Reject</x-admin.action-button></form></div></div></div>@endforeach</div><div class="mt-6">{{ $users->links() }}</div></x-layouts.admin>
