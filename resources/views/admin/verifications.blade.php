<x-layouts.admin title="Verification Requests"><h1 class="text-3xl font-extrabold">Verification Requests</h1><div class="mt-6 space-y-4">@foreach($requests as $request)<div class="rounded-lg border border-borderSoft bg-white p-5 shadow-soft"><div class="font-bold">{{ $request->user->name }}</div><x-admin.status-badge :status="$request->status" /><div class="mt-3 flex gap-2"><form method="POST" action="{{ route('admin.verifications.approve', $request) }}">@csrf<x-admin.action-button>Approve</x-admin.action-button></form><form method="POST" action="{{ route('admin.verifications.reject', $request) }}">@csrf<input type="hidden" name="rejection_reason" value="Submitted files could not be verified."><x-admin.action-button>Reject</x-admin.action-button></form></div></div>@endforeach</div><div class="mt-6">{{ $requests->links() }}</div></x-layouts.admin>
