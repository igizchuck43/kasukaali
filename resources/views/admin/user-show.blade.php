<x-layouts.admin :title="$user->name"><h1 class="text-3xl font-extrabold">{{ $user->name }}</h1><div class="mt-6 grid gap-6 lg:grid-cols-2"><div class="rounded-lg border border-borderSoft bg-white p-6 shadow-soft"><p>{{ $user->email }}</p><p class="mt-2">{{ $user->city }} · {{ $user->relationship_intention }}</p><div class="mt-4"><x-admin.status-badge :status="$user->status" /></div></div><form method="POST" action="{{ route('admin.users.reject', $user) }}" class="rounded-lg border border-borderSoft bg-white p-6 shadow-soft">@csrf<h2 class="font-bold">Reject with reason</h2><textarea name="reason" rows="4" class="mt-3 w-full rounded-lg border border-borderSoft p-3"></textarea><button class="btn-primary mt-4">Reject User</button></form></div></x-layouts.admin>
