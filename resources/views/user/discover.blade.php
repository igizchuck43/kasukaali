<x-layouts.user title="Discover">
    <h1 class="text-3xl font-extrabold">Discover</h1>
    <div class="mx-auto mt-6 max-w-xl">@forelse($profiles as $profile)<x-user.profile-card :user="$profile"><div class="mt-5 grid grid-cols-3 gap-3"><form method="POST" action="{{ route('user.discover.pass', $profile) }}">@csrf<button data-swipe-action class="btn-secondary w-full">Pass</button></form><form method="POST" action="{{ route('user.discover.like', $profile) }}">@csrf<input type="hidden" name="type" value="like"><button data-swipe-action class="btn-primary w-full">Like</button></form><form method="POST" action="{{ route('user.discover.like', $profile) }}">@csrf<input type="hidden" name="type" value="super_like"><button data-swipe-action class="btn-premium w-full">Super</button></form></div></x-user.profile-card>@break @empty<div class="rounded-lg border border-borderSoft bg-white p-8 text-center shadow-soft">No new profiles right now.</div>@endforelse</div>
</x-layouts.user>
