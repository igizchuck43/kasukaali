<x-layouts.admin :title="$title"><h1 class="text-3xl font-extrabold">{{ $title }}</h1><div class="mt-6 rounded-lg border border-borderSoft bg-white shadow-soft"><table class="w-full text-left text-sm"><thead class="bg-surface"><tr>@foreach($columns as $column)<th class="p-4">{{ ucfirst(str_replace('_',' ', $column)) }}</th>@endforeach</tr></thead><tbody>@foreach($items as $item)<tr class="border-t border-borderSoft">@foreach($columns as $column)<td class="p-4">{{ data_get($item, $column) }}</td>@endforeach</tr>@endforeach</tbody></table></div><div class="mt-6">{{ $items->links() }}</div></x-layouts.admin>
