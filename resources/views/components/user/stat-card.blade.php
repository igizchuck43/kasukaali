@props(['label', 'value'])
<div class="rounded-lg border border-borderSoft bg-white p-5 shadow-soft">
    <div class="text-sm font-semibold text-muted">{{ $label }}</div>
    <div class="mt-2 text-2xl font-extrabold text-secondary">{{ $value }}</div>
</div>
