<x-layouts.admin title="Profile Photos"><h1 class="text-3xl font-extrabold">Profile Photos</h1><div class="mt-6 grid gap-5 md:grid-cols-3">@foreach($photos as $photo)<div class="rounded-lg border border-borderSoft bg-white p-3 shadow-soft"><img src="{{ $photo->url() }}" class="h-64 w-full rounded-lg object-cover"><div class="mt-3 font-bold">{{ $photo->user->name }}</div><div class="mt-2"><x-admin.status-badge :status="$photo->status" /></div><div class="mt-3 flex gap-2"><form method="POST" action="{{ route('admin.photos.approve', $photo) }}">@csrf<x-admin.action-button>Approve</x-admin.action-button></form><form method="POST" action="{{ route('admin.photos.reject', $photo) }}">@csrf<x-admin.action-button>Reject</x-admin.action-button></form></div></div>@endforeach</div><div class="mt-6">{{ $photos->links() }}</div></x-layouts.admin>
