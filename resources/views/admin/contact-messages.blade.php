<x-layouts.admin title="Contact Messages"><h1 class="text-3xl font-extrabold">Contact Messages</h1><div class="mt-6 space-y-4">@foreach($messages as $message)<div class="rounded-lg border border-borderSoft bg-white p-5 shadow-soft"><div class="flex justify-between"><div><div class="font-bold">{{ $message->subject }}</div><div class="text-sm text-muted">{{ $message->name }} · {{ $message->email }}</div></div><x-admin.status-badge :status="$message->status" /></div><p class="mt-3">{{ $message->message }}</p><form method="POST" action="{{ route('admin.contact-messages.read', $message) }}" class="mt-3">@csrf<x-admin.action-button>Mark Read</x-admin.action-button></form></div>@endforeach</div><div class="mt-6">{{ $messages->links() }}</div></x-layouts.admin>
