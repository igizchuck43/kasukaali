<x-layouts.admin title="Users">
    <div class="flex items-center justify-between"><h1 class="text-3xl font-extrabold">Users</h1><form><input name="search" value="{{ request('search') }}" placeholder="Search users" class="rounded-full border border-borderSoft px-4 py-2"></form></div>
    <x-admin.table><table class="mt-6 w-full text-left text-sm"><thead class="bg-surface text-muted"><tr><th class="p-4">Name</th><th>Status</th><th>Role</th><th>Actions</th></tr></thead><tbody>@foreach($users as $user)<tr class="border-t border-borderSoft"><td class="p-4"><a class="font-bold text-primary" href="{{ route('admin.users.show', $user) }}">{{ $user->name }}</a><div class="text-muted">{{ $user->email }}</div></td><td><x-admin.status-badge :status="$user->status" /></td><td>{{ $user->role }}</td><td class="flex flex-wrap gap-2 p-4"><form method="POST" action="{{ route('admin.users.approve', $user) }}">@csrf<x-admin.action-button>Approve</x-admin.action-button></form><form method="POST" action="{{ route('admin.users.suspend', $user) }}">@csrf<x-admin.action-button>Suspend</x-admin.action-button></form><form method="POST" action="{{ route('admin.users.ban', $user) }}">@csrf<x-admin.action-button>Ban</x-admin.action-button></form></td></tr>@endforeach</tbody></table></x-admin.table><div class="mt-6">{{ $users->links() }}</div>
</x-layouts.admin>
