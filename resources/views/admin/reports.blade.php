<x-layouts.admin title="Reports"><h1 class="text-3xl font-extrabold">Reports</h1><div class="mt-6 space-y-4">@foreach($reports as $report)<form method="POST" action="{{ route('admin.reports.update', $report) }}" class="rounded-lg border border-borderSoft bg-white p-5 shadow-soft">@csrf @method('PATCH')<div class="font-bold">{{ $report->reportedUser->name }} reported by {{ $report->reporter->name }}</div><div class="text-sm text-muted">{{ $report->reason }}</div><p class="mt-3">{{ $report->description }}</p><select name="status" class="mt-3 rounded-lg border border-borderSoft px-4 py-2"><option>reviewed</option><option>resolved</option><option>dismissed</option></select><input name="admin_notes" placeholder="Moderation notes" class="mt-3 w-full rounded-lg border border-borderSoft px-4 py-2"><button class="btn-primary mt-3">Save Review</button></form>@endforeach</div><div class="mt-6">{{ $reports->links() }}</div></x-layouts.admin>
